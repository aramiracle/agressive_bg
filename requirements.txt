# Core dependencies
torch>=2.0.0
tqdm>=4.65.0

# Web UI (WebSocket server)
websockets>=11.0

# Optional: CUDA support (install separately based on your CUDA version)
# pip install torch --index-url https://download.pytorch.org/whl/cu118

